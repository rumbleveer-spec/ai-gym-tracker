version: '3.8'
services:
  ai-gym-tracker:
    build: .
    container_name: ai-gym-tracker
    restart: unless-stopped
    ports: ["3002:3002"]
    environment: [NODE_ENV=production, PORT=3002]
    env_file: [.env]
networks:
  app-network:
